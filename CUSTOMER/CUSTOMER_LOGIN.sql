CREATE TABLE CUSTOMER_LOGIN
(
    LOGIN_ID NUMBER(10) GENERATED BY DEFAULT AS IDENTITY,
    EMAIL VARCHAR2(50) NOT NULL UNIQUE,
    PASSWORD VARCHAR2(30) NOT NULL,
    CREATED_DATE DATE DEFAULT (SYSDATE),
    PRIMARY KEY (LOGIN_ID)
    
);
